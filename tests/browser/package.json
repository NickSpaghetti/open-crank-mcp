{
  "name": "open-crank-mcp-browser-tests",
  "private": true,
  "type": "module",
  "description": "Browser tests for the byos VNC pages. Run through make test-byos-browser, which uses the Playwright container matching the version pinned here, so nothing is installed on the host.",
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0",
    "@types/node": "24.9.2",
    "@typescript/native-preview": "7.0.0-dev.20260707.2"
  }
}
